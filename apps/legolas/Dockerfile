# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @legolas/legolas`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t legolas-legolas`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system legolas-legolas && \
          adduser --system -G legolas-legolas legolas-legolas

COPY apps/legolas/dist legolas-legolas/
COPY apps/legolas/package.json legolas-legolas/
RUN chown -R legolas-legolas:legolas-legolas .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix legolas-legolas --omit=dev -f install

CMD [ "node", "legolas-legolas" ]
